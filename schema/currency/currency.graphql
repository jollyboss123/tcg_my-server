type Currency {
    code: String!
    numericCode: String!
    fraction: Int!
    grapheme: String!
    template: String!
    decimal: String!
    thousand: String!
}

#sample response
#{decimal: ".", thousand: ",", code: USD, fraction: 2, numericCode: "840", grapheme: "$", template: "$1"}
